export function SupervisorTrace({trace}:{trace:any}){if(!trace)return <aside><h2>Supervisor trace</h2><p>Выберите обработанный ход.</p></aside>;return <aside><h2>Supervisor trace</h2><p>Turn: {trace.turn_id}</p><p>Language: {trace.language}</p><p>Selected: {trace.selected?.map((x:any)=>x.id).join(", ")}</p><p>Alternatives: {trace.alternatives?.map((x:any)=>x.id).join(", ")||"—"}</p><pre>{JSON.stringify({clarification:trace.clarification,fallback:trace.fallback,handoff:trace.handoff,execution:trace.execution,latency_ms:trace.latency_ms,metadata:trace.metadata},null,2)}</pre></aside>}
